# NK Modelling Notebook
These are my notes and visualizations for Stuart Kauffman's *The Origins of Order: Self-Organization and Selection in Evolution*. 

To run this notebook sucessfully, clone the repo, and use uv sync in the folder to load all the requisite packages from the lock file. 
Then run the notebook with `uv run jupyter notebook NK_Model_Notes.ipynb`.
